# CloudGlide OLAP Simulation Framework Dependencies

# Core Data Science Libraries
numpy>=1.21.0
pandas>=1.3.0

# Visualization Libraries
matplotlib>=3.4.0
seaborn>=0.11.0
adjustText>=1.1.1

# System Monitoring
psutil>=5.8.0

# Validation
jsonschema>=4.0.0

# Standard Library Dependencies (included for completeness, typically pre-installed)
# The following are part of Python's standard library but listed for documentation:
# sys, os, json, logging, collections, dataclasses, typing, enum
# itertools, heapq, random, math, subprocess, csv, io, cProfile, pstats, re